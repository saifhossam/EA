# Simulated Annealing Algorithm

This repository contains an implementation of the **Simulated Annealing (SA) algorithm**, a probabilistic technique for approximating the global optimum of a given function.

## Features
- **Simulated Annealing Algorithm**: Uses probabilistic techniques to escape local minima and converge toward a global optimum.
- **Dataset Support**: Reads data from `large.csv` for optimization tasks.
- **Visualization**: Uses `matplotlib` for plotting results.
- **Efficient Computation**: Uses `numpy` for fast numerical operations.

## Installation
1. Clone the repository:
   ```sh
   git clone https://github.com/your-username/your-repo-name.git

## Dataset
- The notebook reads data from large.csv. Ensure the dataset is in the same directory as the notebook.
- The dataset structure should be appropriate for the optimization problem.

## Usage
- Open and run the Jupyter Notebook (Simulated Annealing algorithm 1.ipynb).
- The algorithm will perform optimization based on the given data.
- Visualize the optimization process using plots.
